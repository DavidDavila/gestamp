import { GroupResultsPipe } from './group-results.pipe';

describe('GroupResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
