import { SortCountriesByPipe } from './sort-countries-by.pipe';

describe('SortCountriesByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCountriesByPipe();
    expect(pipe).toBeTruthy();
  });
});
